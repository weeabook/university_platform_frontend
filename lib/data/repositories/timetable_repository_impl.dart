import '../../domain/timetable/timetable_repository.dart';

class TimetableRepositoryImpl implements TimetableRepository {
  @override
  Future<void> downloadTimetable(int groupId) {
    throw UnimplementedError();
  }
}
